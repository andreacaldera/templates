import React from 'react';
import { connect } from 'react-redux';

const Timer = () =>
  (<div>
    <h1>Politics coming soon!</h1>
  </div>);

Timer.propTypes = {
};

export default connect()(Timer);
