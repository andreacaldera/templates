import { NAMESPACE } from '../constants';

export const START = `${NAMESPACE}/timer/START`;
export const STOP = `${NAMESPACE}/timer/STOP`;
export const SET_CONFIG = `${NAMESPACE}/timer/SET_CONFIG`;
export const RUNNING = `${NAMESPACE}/timer/RUNNING`;
export const STOPPED = `${NAMESPACE}/timer/STOPPED`;
